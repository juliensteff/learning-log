\# Day 001 — Mon Jun 22, 2026



\*\*Plan day 1 / Week 1 / Month 1 — Foundations.\*\*



\## Done today

\- CS50P Lecture 0 watched (revisit from Fri, counts as real start).

\- Python 3.14.6 installed, VS Code + Python extension (Pylance) active.

\- First scripts: `cs50p/lecture0/hello.py`, `cs50p/lecture0/calculator.py`.

\- Created `.venv` for the repo, confirmed activation.

\- Verified Git identity routes to `juliensteff` via `includeIf` on `\~/Documents/dev/`.



\## Notes

\- Pylance = LSP for Python in VS Code. Free, bundled with the extension.

\- `venv` isolates per-project deps. Activate every time before `pip install`.

\- f-strings (`f"Hello, {name}"`) feel like the right default for output.



\## Next (Tue Jun 23)

\- CS50P Lecture 1: Conditionals — watch + problem set.

\- Start IBM cert Course 1, lesson 3.

\- One small commit minimum.

